apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "example-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
